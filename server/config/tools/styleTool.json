{
  "name": "custom_style_writer_tool",
  "description": "This tool will write the code of the style using the given information into a common custom css file",
  "input_schema": {
    "type": "object",
    "properties": {
      "custom_style_details": {
        "type": "string",
        "description": "The details of the style to be written in the custom css file"
      },
      "style_file_path": {
        "type": "string",
        "description": "The path of the file to write the code to"
      }
    },
    "required": ["custom_style_details", "style_file_path"]
  }
}
