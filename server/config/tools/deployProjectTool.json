{
  "name": "deploy_project_tool",
  "description": "This tool can be used only when all the reqiured files have been written.",
  "input_schema": {
    "type": "object",
    "properties": {
      "directory": {
        "type": "string",
        "description": "The name of the directory can be passed"
      }
    },
    "required": ["directory"]
  }
}
