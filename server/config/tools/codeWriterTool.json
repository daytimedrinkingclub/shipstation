{
  "name": "code_writer_tool",
  "description": "This tool generates the file as per the provided description and context.",
  "input_schema": {
    "type": "object",
    "properties": {
      "code": {
        "type": "string",
        "description": "The code to be written to the file. Never include comments."
      },
      "description": {
        "type": "string",
        "description": "Always return a detailed summary of the code that has been written in the file, and most importantly all the important tag names, links / references to other files."
      }
    },
    "required": ["code", "description"]
  }
}
