{
  "name": "script_writer_ai_tool",
  "description": "This tool will write the code of the script using the given information",
  "input_schema": {
    "type": "object",
    "properties": {
      "script_details": {
        "type": "string",
        "description": "The details of the scripts to be written in the common scripts.js file with function names, etc"
      },
      "script_file_path": {
        "type": "string",
        "description": "The path of the file to write the code to"
      }
    },
    "required": ["script_details", "script_file_path"]
  }
}
