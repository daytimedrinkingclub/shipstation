{
  "name": "ai_research_assistant",
  "description": "This tool will search for information on the web",
  "input_schema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The query to search and return the top 3 links and available information about the query"
      }
    },
    "required": ["query"]
  }
}
